# Moved to tasks/memory.md

`tasks/memory.md` is the canonical gwt memory log.

This file remains only as a legacy pointer for agents or tools that still look
for `tasks/lessons.md`. New reusable lessons, decisions, failure patterns, and
agent workflow corrections should be added to `tasks/memory.md` with `Type`,
`Context`, `Learning`, and `Future Action` fields.
