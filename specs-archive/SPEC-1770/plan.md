# Plan: SPEC-1770 — rebuilt TUI interaction policy

## Summary

Replace the old Agent Canvas interaction assumptions with a keyboard-first policy for `Branches`, the session selector, `equal grid / maximize` session workspace, and the tabbed management workspace.

## Phased Implementation

1. Define `Branches` navigation and `Enter` branching.
2. Define session selector and session workspace focus / maximize operations.
3. Define management workspace open/close and tab navigation.
4. Reconfirm that terminal wheel/drag/paste interactions still match `SPEC-1541`.
