1. Inspect the current implementation paths and tests related to `機能仕様: HUD＆UIシステム (RTS風コンソール・通知・設定メニュー)`.
2. Add or update RED tests from the `TDD` section before changing production code.
3. Implement the minimum behavior needed to satisfy the listed FR / SC items.
4. Run Unity compile checks, relevant EditMode / PlayMode tests, and touched-code coverage verification.
