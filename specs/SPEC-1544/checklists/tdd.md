### EditMode tests
- `GhCommandRunner`
  - JSON command success
  - non-zero exit で `GhCliException`
  - timeout / cancellation
- `GitHubService`
  - `CheckAuthAsync`
  - issue list/view mapping
  - issue create/edit/close/comment command generation
  - PR list/view mapping
  - PR create / merge flag mapping
  - CI checks mapping
- DTO -> model mapping
  - labels
  - assignees
  - milestone
  - review comments
  - linked issue extraction
  - check suite status/conclusion
- Lead PR操作
  - Lead経由のPR作成がタイトルフォーマット `[Lead] {task.Title}` に従うこと
  - Mergeable判定が正しく動作すること
- クラッシュレポート
  - オプトインOFF時にIssue作成がスキップされること
  - クラッシュレポートIssueのフォーマットが正しいこと

### Integration RED tests
- `gh auth status` 未認証時のハンドリング
- repo root が異なる場合の command workingDir
- large issue bodies / markdown comment bodies
- required checks が空 or null の PR
