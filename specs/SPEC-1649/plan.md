1. #1607 を本 SPEC の PR status wording clarification として統合する
2. Rust の `compute_merge_ui_state` に RED テストを追加する
3. Svelte の `PrStatusSection` / Sidebar 系に RED テストを追加する
4. 必須 checks 実行中のみ `checking` に落とすように合成ロジックを修正する
5. 関連 UI が更新後の `mergeUiState` を正しく反映することを確認する
6. `cargo test` と `pnpm test` の対象範囲を実行し、Issue を更新する

---
