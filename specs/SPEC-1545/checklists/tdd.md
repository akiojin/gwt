### EditMode tests
- `AgentDetector`
  - `FindInPath`
  - detect available/unavailable agents
  - version command parsing
- command building
  - Claude / Codex / Gemini / OpenCode / GithubCopilot / Custom
  - custom profile の `DefaultArgs` と `WorkdirArgName`
- `AgentSessionData`
  - serialization round-trip
  - `AutoPrCreated` default false
  - same worktree with multiple agents is allowed by design
- `AgentService`
  - `HireAgentAsync` が PTY spawn + pane add + session save を行う
  - `FireAgentAsync` が PTY kill + pane remove + status update を行う
  - `SendInstructionAsync` が PTY write + history append を行う
  - `RestoreSessionAsync` が stopped 状態で復元する
  - `ListSessionsAsync` が projectRoot で filter する
  - `ActiveSessionCount` が running/idle/waiting_input を数える

### Integration / orchestration tests
- `LeadOrchestrator`
  - candidate defaults
  - unique personality
  - voice key presence
  - handover document generation
  - monitoring loop で idle agent に pending task を割り当てる
- pending RED tests
  - app exit で all agents graceful shutdown
  - `JobType` persisted to session
  - DevContainer launch path
  - `gh` / Docker 依存エラー時の toast or state fallback
