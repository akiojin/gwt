- `voiceInputController.test.ts`
  - 固定 PTT キーで keydown / keyup が start / stop に結び付くこと
  - 非固定キーでは開始しないこと
  - button 側の press/release API が PTT 開始 / 停止を行うこと
- `TerminalView.test.ts`
  - Voice ボタンが `gwt-voice-ptt-start` / `gwt-voice-ptt-stop` を dispatch すること
  - `available=false` でも enabled/supported なら押せること
  - preparing 中は無効であること
- `SettingsPanel.test.ts` / `settingsPanelHelpers.test.ts`
  - hotkey 欄がなくても normalize / save が成立すること
- Rust settings tests
  - hotkey フィールドなしで round-trip できること
  - 旧 config に hotkey があってもロードできること
- Playwright
  - Voice Input タブの固定 PTT 表示と保存 payload を確認すること

---
