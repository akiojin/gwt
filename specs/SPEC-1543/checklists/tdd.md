### EditMode tests
- porcelain parser 系
  - worktree list
  - branch list
  - commit log
  - shortstat
  - status porcelain
  - change summary
  - stash list
- `GitCommandRunner`
  - stdout/stderr/exitCode を取得できる
  - timeout / cancellation で kill される
- `GitService`
  - list worktrees
  - create/delete worktree
  - list branches / current branch
  - change summary / change stats
  - branch meta ahead/behind
  - repo type detection
  - diff / stash / tags / base branch candidates
  - cleanup candidate 抽出
- Lead Git権限
  - force push コマンドが拒否されること
  - rebase コマンドが拒否されること
  - worktree作成→push→PR作成→merge→worktree削除のフローが許可されること

### Integration RED tests
- 実 git repo fixture を使った worktree add/remove
- bare repo / linked worktree / empty dir の判定
- path quoting を含む branch/worktree path
- `git` 非存在時の失敗処理
