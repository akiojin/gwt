1. gwt で target project/worktree を選ぶ
2. agent を launch する
3. Codex では `./.codex/skills`、Claude Code では `./.claude/*`、Gemini では `./.gemini/skills` を確認する
4. spec が必要なら `gwt-project-index` で検索し、`gwt-issue-spec-ops` で更新する
