1. launch 時 registration の対象 root を launch target worktree へ揃える
2. Claude/Codex/Gemini すべてで direct local deployment の gwt skill bundle を使う
3. bundle asset の path rewrite と health check を揃える
4. local `specs/` 依存を Issue-first 読み出しに置き換える
5. gwt リポジトリから `specs/specs.md` と `specs/archive/` を削除する
