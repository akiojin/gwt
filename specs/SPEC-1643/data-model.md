- `search_github_issue_catalog` は full issue rows を返す unified search 用 command とする
- semantic spec search は既存 `search_github_issues_cmd` を利用する
- frontend は browse result / catalog result / semantic result を issue number で統合表示する

---
