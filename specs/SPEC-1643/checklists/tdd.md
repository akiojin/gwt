- `IssueListPanel` で unified search が catalog search と semantic spec search を併用するテスト
- `IssueListPanel` で spec index 更新操作が Git 側に存在するテスト
- `ProjectIndexPanel` が files-only になるテスト
- Rust helper / Tauri command が unified search 用 API を提供するコンパイル確認

---
