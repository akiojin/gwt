- `ProjectIndexPanel` の `Issues` タブは `search_github_issues_cmd` を使っていたが、実装上は gwt-spec index が主対象だった
- `#1684` のタイトルは `Issue/PR Index` だが、現行コードに独立した PR index UI は存在しなかった
- `#1650` は file index 専用の責務として維持するのが自然

---
