1. `#1684` の canonical ownership を `#1643` へ統合する
2. Git 側 `Issues/Specs` 画面に unified search を維持・整理する
3. Issue タブ詳細フローの責務を `#1354` に固定する
4. Files index / recovery の責務を `#1520` に固定する
5. Issue タイトル解決と local cache の upstream dependency を `#1714` に固定する
6. フロントテストと Rust コンパイル確認で回帰を防ぐ
