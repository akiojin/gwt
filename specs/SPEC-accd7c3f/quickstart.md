1. Update `issue_spec.rs` to prefer `doc:*` artifacts.
2. Keep `SpecIssueDetail.sections` stable for the frontend.
3. Extend command/tooling paths to support `doc` artifacts.
4. Support migration from old body bundles and local `specs/SPEC-*`.
