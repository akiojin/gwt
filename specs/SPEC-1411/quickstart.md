1. `terminal.rs` の 5 つの match に copilot アーム追加
2. `agents.rs` に `detect_copilot()` + vec 登録
3. `agentUtils.ts` の `AgentId` 型と `inferAgentId` に copilot 追加
4. `agentLaunchFormHelpers.ts` の `supportsModelFor` に copilot 追加
5. `AgentLaunchForm.svelte` の `modelOptions` に copilot モデル一覧追加
