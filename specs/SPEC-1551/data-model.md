### Runtime Models
- `VoiceSettings`
  - `enabled: bool`
  - `pushToTalkBinding: string`
  - `sttEndpoint: string`
  - `ttsEndpoint: string`
  - `voiceId: string`
  - `inputGain: float`
- `VoiceCaptureState`
  - `isRecording: bool`
  - `startedAt: string?`
  - `lastTranscript: string`
- `TtsPlaybackState`
  - `isSpeaking: bool`
  - `voiceId: string`
  - `currentText: string`
- `VoiceNotificationEvent`
  - `eventType: string` (pr_merge, ci_failure, task_complete, lead_question, new_issue, level_up)
  - `messageText: string`

### Service Contracts
- `IVoiceService`
  - `StartRecordingAsync(ct)` ? transcript ???
  - `StopRecording()` ????????????
  - `SpeakAsync(text, voiceId, ct)` ? TTS ???????????????
  - `StopSpeaking()` ???????????
  - `NotifyEventAsync(eventType, messageText, ct)` ????????TTS?????????
- `IAudioService` / `ISoundService`
  - TTS ???????????????????

### Persistence
- ????? `Settings.VoiceInput` ??????
- TTS ??? voice id ? Lead ????? `VoiceKey` ???????????????????

### External Payloads
- STT request
  - `audio bytes`
  - `model`
  - `language`
- STT response
  - `text`
- TTS request
  - `text`
  - `voice`
  - `model`
- TTS response
  - `audio bytes`
