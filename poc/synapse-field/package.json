{
  "name": "synapse-field-poc",
  "private": true,
  "type": "module",
  "devDependencies": {
    "http-server": "^14.1.1",
    "rimraf": "^6.0.1"
  },
  "scripts": {
    "build": "rimraf dist && bun build ./main.ts --target=browser --outdir=./dist --minify && cp index.html styles.css ./dist/",
    "serve": "bunx --bun http-server ./dist -p 3400 -c-1",
    "dev": "bun run build && bun run serve"
  }
}
