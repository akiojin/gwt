{
  "name": "synapse-field-poc",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && bun build ./main.ts --target=browser --outdir=./dist --minify && cp index.html styles.css ./dist/",
    "serve": "bunx --bun http-server ./dist -p 3400 -c-1",
    "dev": "bun run build && bun run serve"
  }
}
