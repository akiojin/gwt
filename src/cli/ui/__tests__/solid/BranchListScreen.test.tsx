import { describe, it } from "bun:test";

describe("Solid BranchListScreen", () => {
  it.todo("moves selection and selects branch on enter");
  it.todo("filters branches by query");
  it.todo("scrolls when selection moves beyond visible window");
});
